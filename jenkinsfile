pipeline{
    agent any
    tools {
        maven 'maven'
        jdk 'JDK'
    }
    environment{
        SNAPREPO = 'vpro-snapshots'
        NEXUSUSER = 'admim'
        NEXUSPASSWORD = 'admin'

    }
    stages {
        stage(BUILD) {
            steps{
                sh 'mvn install -DeskipTests'
            }
        }
    }
}